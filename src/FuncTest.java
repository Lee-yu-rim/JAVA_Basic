
public class FuncTest {

	public static void main(String[] args) {
		
		Func f = new Func();
		
//		f.add();    //함수의 호출 -> 함수가 가진 실행문을 실행하라는 요청
//		f.add2(10,20);  //호출과 동시에 x와 y에 값을 넣게 됨.
//		f.add2(100,200);  //함수는 필요할 때마다 재사용이 가능
//		System.out.println(f.add3(5, 8)); 
//		int sum = f.add3(15,20);  //호출한 값이 데이터가 되므로 sum이라는 값에 다시 넣어지게 된다?
//		System.out.println(sum);
		
//		int sub = f.sub(20, 5);
//		int mul = f.mul(10, 5);
//		int div = f.div(15, 5);
//		System.out.println("sub = " + sub);
//		System.out.println("mul = " + mul);
//		System.out.println("div = " + div);
		
//		f.test(10,20);  //x값이 y값보다 작기 때문에 실행되지 않는다.
//		f.test(20, 10); //x값이 y값보다 크기 때문에 실행됨
		
		
//		int result = f.test2(10, 20);
//		System.out.println(result);   //20
//		int result2 = f.test2(20, 10);
//		System.out.println(result2);  //20
		
		f.call1();  // call1 call2 call3
		//함수는 다른 함수를 호출하는 기능을 가진다. -> 메소드 안에 다른 함수를 호출하도록 입력하면 한번의 호출로 여러 함수를 호출할 수 있다.
		
		
		
		
		
		
		
		
		
		
		
		
		
		
		
		
		
		
		

	}

}
